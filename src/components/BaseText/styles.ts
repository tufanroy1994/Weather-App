import {StyleSheet} from 'react-native';
import {AppColors, FontSizes} from '../../utils';

export const styles = StyleSheet.create({
  text: {
    color: AppColors.PRIMARY_TEXT,
    fontSize: FontSizes.FONT_SIZE_35,
    fontWeight: 'bold',
  },
});
